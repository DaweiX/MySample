<Page
    x:Class="MySample.Pages.Utilities.PDFReader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MySample.Pages.Utilities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel Margin="8">
                <TextBlock Text="PDF阅读器" FontSize="36"/>
                <Button Content="选取文档" Click="Button_Click"/>
                <TextBox x:Name="txt_page" Width="100" HorizontalAlignment="Left"/>
                <Button Content="转到" Click="go_Click"/>
                <ProgressRing x:Name="ring"/>
                <Image x:Name="viewPage" Margin="12"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
