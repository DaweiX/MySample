<Page
    x:Class="MySample.Pages.Controls.Sytles"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MySample.Pages.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel Margin="8">
                <StackPanel.Resources>
                    <Style x:Key="commonStyle" TargetType="Button">
                        <Setter Property="Width" Value="120"/>
                        <Setter Property="Foreground" Value="Green"/>
                        <Setter Property="Background" Value="Wheat"/>
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="样式和模板" FontSize="36" Width="400"/>
                <Button Content="按钮1" Style="{StaticResource commonStyle}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
